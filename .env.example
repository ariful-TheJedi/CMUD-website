# =============================================================================
# Self-hosted MedLearHub / CMUD — copy to `.env` and edit
# Works with: npm run dev | npm start | PM2 (ecosystem.config.cjs)
# =============================================================================

# Local / VM Postgres (required)
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/medlearhub"

# Better Auth (required for admin login)
BETTER_AUTH_SECRET="replace-with-a-long-random-string-32chars+"
# MUST match the exact URL you open in the browser (no trailing slash)
# VPS example: http://192.168.0.113:3000
BETTER_AUTH_URL="http://localhost:8080"
# Extra origins (comma-separated). Use * to also trust the browser Origin header (LAN/VPS).
BETTER_AUTH_TRUSTED_ORIGINS="*"


# Production Node bind (npm start / PM2). Defaults: 0.0.0.0:3000
# HOST=0.0.0.0
# PORT=3000
# NITRO_HOST=0.0.0.0
# NITRO_PORT=3000

# Build target is locked to node-server in vite.config.ts — do not use cloudflare/wrangler.
# NITRO_PRESET=node-server

# App folder (package.json). Never .output. Optional — start-prod sets PROJECT_ROOT=cwd.
# PROJECT_ROOT="/www/wwwroot/your-app"

# -----------------------------------------------------------------------------
# External assets (recommended) — keep media OUT of the app deploy folder
# -----------------------------------------------------------------------------
# Disk path (outside the git/project tree). BOTH media AND attachments live here:
#   cmud-assets/media/...
#   cmud-assets/attachment/...
# Favicon stays on the app at /favicon.png (project public/ or Nginx), not prefixed.
#
# Local Windows sibling example:
#   ASSETS_ROOT="E:/cmud-assets"
# VPS example:
#   ASSETS_ROOT="/www/wwwroot/cmud-assets"
ASSETS_ROOT="E:/cmud-assets"

# Browser URL prefix for /media and /attachment (rebuild required — VITE_*).
# Empty = serve at /media/... and /attachment/... from the Node app.
# With Nginx alias, use the same path prefix:
#   location /cmud-assets/ { alias /www/wwwroot/cmud-assets/; }
#   VITE_ASSETS_PREFIX=/cmud-assets
VITE_ASSETS_PREFIX=/cmud-assets
# Optional server alias of the same value:
# ASSETS_PREFIX=/cmud-assets

# Cloudflare Turnstile (admission + certificate-check).
# Add your domain (e.g. dev.cmudusg.com) in the Cloudflare Turnstile widget settings.
# VITE_* requires a rebuild after changing.
VITE_TURNSTILE_ENABLED=true
VITE_TURNSTILE_SITE_KEY="your-turnstile-site-key"
TURNSTILE_ENABLED=true
TURNSTILE_SECRET_KEY="your-turnstile-secret"

# First admin bootstrap ONLY (npm run auth:seed-admin). Do NOT commit real values.
# Password must be 12+ characters. Remove these from .env after first login if you prefer.
# SEED_ADMIN_EMAIL="you@example.com"
# SEED_ADMIN_PASSWORD="choose-a-strong-unique-password"
# SEED_ADMIN_NAME="Administrator"
